import React from 'react'

export default function Equipment() {
    return (
        <div style={{ padding: 20 }}>
            <h2>Equipment View</h2>
            <p>Equipment page. Connection status and settings for equipment. Can be saved to profiles for later use. Add new equipment profile. Authenticated users only.</p>
        </div>
    );
}