import React from 'react'

export default function Profiles() {
    return (
        <div style={{ padding: 20 }}>
            <h2>Profiles View</h2>
            <p>Equipment and location profiles saved to database. Remove profile. Authenticated users only.</p>
        </div>
    );
}