import React from 'react'

export default function ImageSequencer() {
    return (
        <div style={{ padding: 20 }}>
            <h2>Sequencer View</h2>
            <p>Setup a target sequence, filters, exposure times dither etc. Authenticated users only</p>
        </div>
    );
}