import React from 'react'

export default function TargetSelector() {
    return (
        <div style={{ padding: 20 }}>
            <h2>Find and select Targets</h2>
            <p>Find and select targets. Add to sequencer or plate solve coordinates. Authenticated users only</p>
        </div>
    );
}