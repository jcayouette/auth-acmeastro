import React from 'react'

export default function Settings() {
    return (
        <div style={{ padding: 20 }}>
            <h2>Settings View</h2>
            <p>App configuration, language, layouts, themes. Saved to database under user. Default settings. Authenticated users only.</p>
        </div>
    );
}